{
  "name": "oracle-stack",
  "version": "0.1.0",
  "private": true,
  "description": "Oracle routes a request to a specialist, builds an upgraded Stack, and runs Judge/QA before returning it.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0"
  }
}
