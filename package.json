{
  "name": "oracle-stack",
  "version": "0.4.0",
  "private": true,
  "description": "Adaptive AI execution layer that routes work across models and specialists, quality-checks results, and learns from outcomes.",
  "type": "module",
  "scripts": {
    "start": "node --import ./outreach-fetch-guard.js server.js",
    "dev": "node --import ./outreach-fetch-guard.js --watch server.js",
    "test": "node --test",
    "benchmark": "node scripts/benchmark.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "pg": "^8.13.1"
  }
}
