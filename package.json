{
  "name": "oracle-stack",
  "version": "0.3.0",
  "private": true,
  "description": "Adaptive AI execution layer that routes work across models and specialists, quality-checks results, and learns from outcomes.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "benchmark": "node scripts/benchmark.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "pg": "^8.13.1"
  }
}
